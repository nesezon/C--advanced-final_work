<UserControl x:Class="e_commerce.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:e_commerce"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
  <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
      <TextBlock Text="Логин" FontSize="17" Width="70" TextAlignment="Right" Margin="0,0,20,0"/>
      <TextBox x:Name="Login" FontSize="17" Width="300"></TextBox>
    </StackPanel>
    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
      <TextBlock Text="Пароль" FontSize="17" Width="70" TextAlignment="Right" Margin="0,0,20,0"/>
      <PasswordBox x:Name="Password" FontSize="17" Width="300"></PasswordBox>
    </StackPanel>
    <Button Content="Войти" HorizontalAlignment="Left" Margin="90,0,0,0" Padding="10,0"  FontSize="17" Click="Enter_Click"/>
  </StackPanel>
    <TextBlock x:Name="Message" FontSize="14" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,5"/>
  </Grid>
</UserControl>
